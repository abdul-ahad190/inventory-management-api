# inventory-management-api
A secure Inventory Management REST API built with FastAPI, PostgreSQL, SQLAlchemy, Alembic, and JWT authentication. The API provides CRUD operations for products, categories, suppliers, and users, with protected routes and authentication. API testing is performed using Postman.
